GEMINI_API_KEY=your_key_here
PORT=3001
# Place this file at project root (.env) or server/.env (server/.env wins if both exist)
# Optional: preferred model (others tried automatically if quota/404)
# GEMINI_MODEL=gemini-2.0-flash-lite
# Optional: comma-separated list (overrides default chain)
# GEMINI_MODELS=gemini-2.0-flash-lite,gemini-2.0-flash
# ALLOW_FALLBACK=true
